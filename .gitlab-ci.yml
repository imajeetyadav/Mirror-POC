stages:          # List of stages for jobs, and their order of execution
  - mirror


mirror-job:       # This job runs in the build stage, which runs first.
  stage: mirror
  script:
    - git fetch --all
    - git remote add github git@github.com:imAjeetYadav/Gitlab-mirror-POC.git
    - git push --no-verify --mirror github


